:root {
   --light: #FFFFFFE0;
   --dark: #000000E0;
   --accent: #640;
   --sidebar-select: var(--dark);
   --sidebar-border: var(--sidebar-select);
   --sidebar-background: var(--light);
   --sidebar-color: var(--dark);
   --darkreader-bg--light: var(--dark);
  /* --darkreader-border--dark: var(--light);*/ 	
   --sidebar-left: 3px;
 
}
.sidebar {
   background-image: none !important;
}
.sidebar h1[id*=bg][id*=fg],
.sidebar h2[id*=bg][id*=fg],
.sidebar h3[id*=bg][id*=fg],
.sidebar h4[id*=bg][id*=fg],
.sidebar h5[id*=bg][id*=fg] {
   background-image: none;
   border-left-color: inherit;
}

.remark-visible {
   background: white;
}
/*
html[data-darkreader-scheme="dark"] .remark-slide-content {
   background-color: #000000;
}
*/

html[data-darkreader-mode="dynamic"] .remark-visible,
html[data-darkreader-mode="dynamic"] .sssidebar {
  background-color: var(--darkreader-selection-text);
}
/*
.left-column.sidebar {
   background-color: #00000004;
   height: 200vh;
   margin-top: -924px;
   padding-top: 924px;
}
.remark-slide-scaler {
   overflow: visible;
}
@media print {
   .remark-slide-scaler {
      overflow: hidden;
   }
}
*/
.remark-slide h1, .remark-slide h2, .remark-slide h3, .remark-slide h4, .remark-slide h5  {
   color: var(--accent); /* mainbar */
}
.remark-slide .customtitle .mainbar p {
   font-size: 62%;
}
/*
.remark-slide .customtitle .author,
.remark-slide .customtitle .institute {
   font-size: 80%;
}
.remark-slide .customtitle .institute {
   font-size: 60%;
}
.remark-slide .customtitle p .what,
.remark-slide .customtitle p .where,
.remark-slide .customtitle p .when {
   font-size: 80%;
}
*/	
.remark-slide .metadata .title {
   font-size: 230%;
}
.sidebar h1 + h1, .sidebar h2 + h1, .sidebar h3 + h1 {
   margin-top: 2em !important;
}
